import Domov from './pages/Domov';
import Layout from './Layout.jsx';


export const PAGES = {
    "Domov": Domov,
}

export const pagesConfig = {
    mainPage: "Domov",
    Pages: PAGES,
    Layout: Layout,
};